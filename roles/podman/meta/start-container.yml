- name: "Start {{ container.name }} container"
  become: true
  become_user: "{{ user.name }}"
  ansible.builtin.systemd_service:
    daemon_reload: true
    name: "{{ container.service }}"
    scope: "{{ 'system' if user.uid == 0 else 'user' }}"
    state: started
